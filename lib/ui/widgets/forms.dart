import 'package:flutter/material.dart';

class EspacioForm extends StatelessWidget {
  const EspacioForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 5.0);
  }
}
